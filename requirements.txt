# Experiment 02: Metamorphic Testing on Project CodeGuard (MCP-based)
# Python 3.11+

# Core
openai>=1.0
python-dotenv

# LLM Agent framework
langchain>=0.3
langchain-openai>=0.3
#langchain-ollama>=0.3
langgraph>=0.2

# Local DelftBlue HuggingFace backend
transformers>=4.45
accelerate>=0.34
bitsandbytes>=0.43

# MCP (Model Context Protocol)
mcp>=1.0
nest-asyncio

# Dataset
datasets
pandas

# Static analysis
semgrep

# Rule parsing
pyyaml

# Mutation strategies — bd-03k
nlpaug>=1.1.11              # SynonymReplacementMutator (WordNet), AddRandomWordMutator (RoBERTa)
nltk>=3.8                   # WordNet corpus data for nlpaug.SynonymAug

# Mutation quality validation — bd-03k
sentence-transformers>=2.2  # all-mpnet-base-v2; semantic similarity (Criterion 2)
textstat>=0.7               # Flesch-Kincaid readability grade (Criterion 5, informational)
